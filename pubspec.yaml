name: discunio
description: Ein Trainingsbegleiter für Android und Web
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.1
  sqflite: ^2.3.2
  path: ^1.9.0

  # Optional: Für besseres Material 3 Design (nicht zwingend nötig, aber empfohlen)
  flutter_material_symbols: ^0.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
